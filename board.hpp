#include <iostream>
#include <vector>
#include <string>
using std::vector;
#ifndef BOARD_H
#define BOARD_H
class board {
public:
  board();
  board(int x, int y);
private:
  vector<vector<String>> Board;
};
#endif
